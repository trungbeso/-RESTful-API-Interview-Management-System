spring.application.name=Interview-Management-System

# Database configuration - SQL Server
spring.datasource.url=jdbc:sqlserver://${DB_HOST}:${DB_PORT};databaseName=${DB_NAME};encrypt=true;trustServerCertificate=true;loginTimeout=30;
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

# Hibernate configuration
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update

# Swagger configuration
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html

# JWT Configuration
jwt.secret=${YOUR_SECRET_KEY}
jwt.expiration=3600

# EMAIL SENDER
spring.mail.from=${YOUR_EMAIL_SERVER}
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${YOUR_EMAIL_SERVER}
spring.mail.password=${YOUR_EMAIL_SERVER_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

spring.thymeleaf.mode=HTML
spring.thymeleaf.cache=false

# Email Template Configuration
nip.email.template.account-information.name=account-information
nip.email.template.forgot-password.name=forgot-password

#frontend config
nip.frontend.url=${YOUR_FRONTEND_URL}

logging.level.org.springframework.security=DEBUG

